// 主页：应用默认视图。当前为空白占位，后续填充真实首页内容
// （如 LCU 状态概览、最近对局、快捷入口等）。
export function HomePage() {
  return (
    <div className="flex h-full items-center justify-center">
      <p className="text-sm text-app-muted">主页 · 内容待填充</p>
    </div>
  );
}
